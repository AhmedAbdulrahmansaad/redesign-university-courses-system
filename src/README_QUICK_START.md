# 🚀 دليل البدء السريع

## ✅ ما تم إصلاحه

### 1️⃣ إزالة زر الحساب التجريبي
- ✅ تم حذف زر "🎭 حساب تجريبي للاختبار"
- ✅ يجب على المستخدمين إنشاء حساب حقيقي فقط

### 2️⃣ منع التسجيل المتكرر
- ✅ المستخدم المسجل لا يستطيع الوصول لصفحة Login/Signup
- ✅ إعادة توجيه تلقائية للوحة التحكم المناسبة

### 3️⃣ إصلاح عرض بيانات الطالب
- ✅ البيانات تُحفظ في جدول `students`
- ✅ تظهر في لوحة التحكم بشكل صحيح

---

## 📋 خطوات الإعداد

### الخطوة 1: إعداد قاعدة البيانات

1. افتح **Supabase Dashboard**
2. اذهب إلى **SQL Editor**
3. افتح ملف `database_setup.sql` من المشروع
4. انسخ **كل المحتوى** (بدون علامات ```)
5. الصق في SQL Editor
6. اضغط **Run** ▶️

**⚠️ مهم:** لا تنسخ علامات ```sql أو ``` - فقط الكود النظيف!

### الخطوة 2: تحقق من النجاح

1. افتح ملف `verify_database.sql`
2. انسخ الاستعلامات
3. شغلها في SQL Editor
4. يجب أن ترى:
   - ✅ 7 جداول
   - ✅ 5 أقسام في `departments`

### الخطوة 3: اختبر النظام

1. سجل حساب طالب جديد
2. أدخل جميع البيانات:
   - الاسم
   - البريد @kku.edu.sa
   - الرقم الجامعي (9 أرقام)
   - **التخصص** ← مهم!
   - **المستوى** (1-8) ← مهم!
   - المعدل (اختياري)
3. سجل دخول
4. تحقق من لوحة التحكم

---

## 🔍 حل المشاكل

### المشكلة: خطأ في تنفيذ SQL
**السبب:** نسخت الكود مع علامات ```
**الحل:** انسخ من ملف `database_setup.sql` مباشرة

### المشكلة: البيانات لا تظهر
**الحل:** شغل هذا في SQL Editor:
```sql
SELECT u.name, s.level, s.major, s.gpa
FROM users u
JOIN students s ON s.user_id = u.id
WHERE u.email = 'بريدك@kku.edu.sa';
```

### المشكلة: لا يستطيع التسجيل
**الحل:** تأكد من وجود بيانات في جدول departments:
```sql
SELECT * FROM departments;
```

---

## 📁 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `database_setup.sql` | كود إنشاء جميع الجداول (استخدم هذا!) |
| `verify_database.sql` | استعلامات التحقق |
| `SUPABASE_DATABASE_SETUP.md` | الدليل الكامل المفصل |

---

## ✨ النتيجة المتوقعة

بعد الإعداد الصحيح:

### في لوحة التحكم ستظهر:
- ✅ اسم الطالب الحقيقي
- ✅ المستوى الصحيح (1-8)
- ✅ التخصص الكامل بالعربي/إنجليزي
- ✅ المعدل التراكمي
- ✅ إحصائيات المقررات

### عند محاولة تسجيل دخول مرة أخرى:
- ❌ لن يظهر زر "حساب تجريبي"
- ✅ إذا كنت مسجل دخول وحاولت الذهاب لـ /login → توجيه تلقائي للـ Dashboard

---

## 🎯 أسئلة شائعة

**س: هل أحتاج لحذف البيانات القديمة؟**
ج: لا، الكود يستخدم `CREATE TABLE IF NOT EXISTS` - آمن تماماً

**س: ماذا لو نسيت تشغيل جزء من الكود؟**
ج: يمكنك تشغيل الكود كاملاً مرة أخرى - لن يحدث تكرار

**س: كيف أحذف جميع البيانات وأبدأ من جديد؟**
ج: شغل هذا بحذر:
```sql
DROP TABLE IF EXISTS course_offerings CASCADE;
DROP TABLE IF EXISTS courses CASCADE;
DROP TABLE IF EXISTS admins CASCADE;
DROP TABLE IF EXISTS supervisors CASCADE;
DROP TABLE IF EXISTS students CASCADE;
DROP TABLE IF EXISTS users CASCADE;
DROP TABLE IF EXISTS departments CASCADE;
```
ثم شغل `database_setup.sql` مرة أخرى.

---

## 📞 الدعم

إذا واجهتك مشكلة:
1. تحقق من Console في المتصفح (F12)
2. ابحث عن أخطاء حمراء
3. راجع ملف `SUPABASE_DATABASE_SETUP.md` للتفاصيل

---

## ✅ Checklist النهائي

- [ ] نفذت `database_setup.sql` بنجاح
- [ ] رأيت 7 جداول في Supabase
- [ ] رأيت 5 أقسام في `departments`
- [ ] سجلت حساب طالب جديد
- [ ] ظهرت البيانات في لوحة التحكم
- [ ] لا أستطيع الوصول لـ /login وأنا مسجل دخول

---

**🎉 مبروك! النظام جاهز للاستخدام الآن**
