# 🔧 **دليل حل المشاكل والأخطاء الشائعة - Troubleshooting Guide**

## 🎯 **الهدف:**
دليل سريع لحل المشاكل والأخطاء الشائعة التي قد تظهر

---

## ❗ **المشاكل الشائعة وحلولها**

### **1️⃣ مشكلة: صفحة التعهد لا تظهر**

#### **الأعراض:**
```
❌ عند فتح النظام، لا تظهر صفحة التعهد
❌ يظهر صفحة بيضاء
```

#### **السبب:**
```
- localStorage قد يحتوي على بيانات قديمة
- agreementAccepted = 'true' موجود بالفعل
```

#### **الحل:**
```javascript
// افتح Console (F12) واكتب:
localStorage.clear();
location.reload();

// أو
localStorage.removeItem('agreementAccepted');
location.reload();
```

#### **التحقق:**
```
✅ يجب أن تظهر صفحة التعهد الآن
```

---

### **2️⃣ مشكلة: تسجيل الدخول لا يعمل**

#### **الأعراض:**
```
❌ رسالة خطأ: "حدث خطأ أثناء تسجيل الدخول"
❌ لا يتم الانتقال للصفحة الرئيسية
```

#### **الأسباب المحتملة:**

##### **أ. Backend غير متاح:**
```javascript
// افتح Console وتحقق:
console.error('Failed to fetch');
console.error('NetworkError');
```

**الحل:**
```
1. تأكد من اتصال الإنترنت
2. تحقق من Supabase Project ID
3. تحقق من SUPABASE_ANON_KEY
```

##### **ب. المستخدم غير موجود:**
```
الرسالة: "User not found"
```

**الحل:**
```
1. تأكد من التسجيل أولاً
2. تحقق من البريد الإلكتروني
3. استخدم البيانات الصحيحة
```

##### **ج. كلمة المرور خاطئة:**
```
الرسالة: "Invalid credentials"
```

**الحل:**
```
1. تأكد من كلمة المرور
2. حاول إعادة التسجيل
```

---

### **3️⃣ مشكلة: التسجيل في المقررات لا يعمل**

#### **الأعراض:**
```
❌ رسالة خطأ: "حدث خطأ أثناء التسجيل"
❌ المقرر لا يضاف للجدول
```

#### **الأسباب المحتملة:**

##### **أ. تجاوز الحد الأقصى (20 ساعة):**
```
الرسالة: "تجاوزت الحد الأقصى المسموح"
```

**الحل:**
```
1. احذف بعض المقررات
2. لا تتجاوز 20 ساعة
```

##### **ب. تعارض في الجدول:**
```
الرسالة: "يوجد تعارض في الجدول"
```

**الحل:**
```
1. تحقق من الأوقات
2. اختر مقرر في وقت آخر
```

##### **ج. المقرر ممتلئ:**
```
الرسالة: "المقرر ممتلئ"
```

**الحل:**
```
1. اختر section آخر
2. انتظر حتى يتوفر مكان
```

---

### **4️⃣ مشكلة: الحركات بطيئة أو متقطعة**

#### **الأعراض:**
```
❌ الحركات ليست سلسة
❌ التأخير واضح
```

#### **الأسباب:**

##### **أ. المتصفح قديم:**
```
الحل:
1. حدّث المتصفح لآخر إصدار
2. استخدم Chrome أو Firefox الحديث
```

##### **ب. الجهاز ضعيف:**
```
الحل:
1. أغلق التطبيقات الأخرى
2. استخدم وضع Incognito
3. أعد تشغيل المتصفح
```

##### **ج. GPU Acceleration معطلة:**
```javascript
// في Chrome:
chrome://settings/system
☑️ تفعيل "استخدام تسريع الأجهزة"
```

---

### **5️⃣ مشكلة: الخطوط لا تظهر بشكل صحيح**

#### **الأعراض:**
```
❌ الخطوط العربية غير واضحة
❌ الخطوط الإنجليزية خاطئة
```

#### **الحل:**
```css
/* تأكد من تحميل الخطوط في globals.css */
@import url('https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap');
```

---

### **6️⃣ مشكلة: الألوان خاطئة**

#### **الأعراض:**
```
❌ الأخضر ليس #184A2C
❌ الذهبي ليس #D4AF37
```

#### **الحل:**
```css
/* تحقق من globals.css */
:root {
  --kku-green: #184A2C;
  --kku-gold: #D4AF37;
}
```

---

### **7️⃣ مشكلة: صفحة التعهد تظهر معها Header**

#### **الأعراض:**
```
❌ Header/Navigation/Footer يظهرون في صفحة التعهد
```

#### **السبب:**
```
- الشرط غير موجود في App.tsx
```

#### **الحل:**
```tsx
// في App.tsx:
{currentPage !== 'accessAgreement' && <Header />}
{currentPage !== 'accessAgreement' && <Navigation />}
{currentPage !== 'accessAgreement' && <Footer />}
```

✅ **تم إصلاحه بالفعل!**

---

### **8️⃣ مشكلة: Dark Mode لا يعمل**

#### **الأعراض:**
```
❌ الضغط على زر الثيم لا يغير الألوان
```

#### **الحل:**
```javascript
// تحقق من localStorage:
localStorage.setItem('theme', 'dark');
location.reload();

// أو
localStorage.setItem('theme', 'light');
location.reload();
```

---

### **9️⃣ مشكلة: اللغة لا تتغير**

#### **الأعراض:**
```
❌ الضغط على زر اللغة لا يغير النصوص
```

#### **الحل:**
```javascript
// تحقق من localStorage:
localStorage.setItem('language', 'ar');
location.reload();

// أو
localStorage.setItem('language', 'en');
location.reload();
```

---

### **🔟 مشكلة: Backend لا يستجيب**

#### **الأعراض:**
```
❌ Error: Failed to fetch
❌ Error: Network request failed
```

#### **الأسباب المحتملة:**

##### **أ. Supabase Project غير موجود:**
```
الحل:
1. تحقق من /utils/supabase/info.tsx
2. تأكد من projectId صحيح
3. تأكد من publicAnonKey صحيح
```

##### **ب. Edge Function غير deployed:**
```
الحل:
1. Deploy Edge Function
2. تحقق من Supabase Dashboard
3. تحقق من Logs
```

##### **ج. CORS Policy:**
```
Error: "CORS policy blocked"

الحل:
// في server/index.tsx:
import { cors } from 'npm:hono/cors';
app.use('*', cors());
```

---

## 🐛 **أخطاء Console الشائعة**

### **1. Warning: Each child in a list should have a unique "key" prop**

```tsx
// الخطأ:
{items.map((item) => <div>{item.name}</div>)}

// الصحيح:
{items.map((item) => <div key={item.id}>{item.name}</div>)}
```

---

### **2. Warning: Can't perform a React state update on an unmounted component**

```tsx
// الحل:
useEffect(() => {
  let mounted = true;
  
  async function fetchData() {
    const data = await fetch(...);
    if (mounted) {
      setData(data);
    }
  }
  
  fetchData();
  
  return () => {
    mounted = false;
  };
}, []);
```

---

### **3. Error: Maximum update depth exceeded**

```tsx
// الخطأ:
useEffect(() => {
  setState(newValue); // بدون dependency array
});

// الصحيح:
useEffect(() => {
  setState(newValue);
}, []); // أو dependencies محددة
```

---

## 🔍 **أدوات التشخيص**

### **1. فحص Console:**
```javascript
// افتح Console (F12) وتحقق من:
console.log('Current Page:', currentPage);
console.log('Is Logged In:', isLoggedIn);
console.log('User Info:', userInfo);
console.log('Agreement Accepted:', localStorage.getItem('agreementAccepted'));
```

### **2. فحص localStorage:**
```javascript
// عرض كل localStorage:
for (let i = 0; i < localStorage.length; i++) {
  const key = localStorage.key(i);
  console.log(key, ':', localStorage.getItem(key));
}
```

### **3. فحص Network:**
```
1. افتح DevTools (F12)
2. اذهب لـ Network tab
3. راقب الطلبات
4. تحقق من Status Codes:
   ✅ 200: نجح
   ❌ 400: Bad Request
   ❌ 401: Unauthorized
   ❌ 404: Not Found
   ❌ 500: Server Error
```

---

## 🔧 **أوامر إعادة التعيين**

### **إعادة تعيين كاملة:**
```javascript
// في Console:
localStorage.clear();
sessionStorage.clear();
location.reload();
```

### **إعادة تعيين المستخدم فقط:**
```javascript
localStorage.removeItem('userInfo');
localStorage.removeItem('agreementAccepted');
sessionStorage.clear();
location.reload();
```

### **إعادة تعيين المقررات:**
```javascript
localStorage.removeItem('registeredCourses');
location.reload();
```

---

## 📊 **جدول الأخطاء وحلولها السريعة**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
المشكلة                          الحل السريع
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
صفحة بيضاء                      localStorage.clear() + F5
تسجيل الدخول لا يعمل             تحقق من البيانات + اتصال الإنترنت
الحركات بطيئة                   تحديث المتصفح + إغلاق التطبيقات
الخطوط خاطئة                     تحقق من Google Fonts
الألوان خاطئة                    تحقق من CSS Variables
Header في صفحة التعهد            ✅ تم الإصلاح بالفعل
Dark Mode لا يعمل                F12 → localStorage.setItem('theme', 'dark')
اللغة لا تتغير                   F12 → localStorage.setItem('language', 'ar')
Backend لا يستجيب                تحقق من Supabase + Internet
المقررات لا تسجل                 تحقق من الشروط (ساعات، تعارض)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🆘 **طلب المساعدة**

### **معلومات مطلوبة:**
```
1. وصف المشكلة بالتفصيل
2. الخطوات للوصول للمشكلة
3. لقطة شاشة من Console (F12)
4. لقطة شاشة من Network tab
5. المتصفح والإصدار
6. نظام التشغيل
```

### **نموذج بلاغ الخطأ:**
```markdown
### المشكلة:
[وصف المشكلة]

### الخطوات:
1. [الخطوة 1]
2. [الخطوة 2]
3. [الخطوة 3]

### النتيجة المتوقعة:
[ماذا كنت تتوقع أن يحدث]

### النتيجة الفعلية:
[ماذا حدث فعلاً]

### لقطات الشاشة:
[أرفق لقطات الشاشة]

### البيئة:
- المتصفح: Chrome 120
- نظام التشغيل: Windows 11
- الجهاز: Desktop
```

---

## ✅ **التحقق من سلامة النظام**

### **Checklist سريع:**
```javascript
// افتح Console واكتب:

// 1. تحقق من المتغيرات العامة:
console.log('projectId:', projectId);
console.log('publicAnonKey:', publicAnonKey ? 'موجود' : 'مفقود');

// 2. تحقق من localStorage:
console.log('agreementAccepted:', localStorage.getItem('agreementAccepted'));
console.log('userInfo:', localStorage.getItem('userInfo'));
console.log('language:', localStorage.getItem('language'));
console.log('theme:', localStorage.getItem('theme'));

// 3. تحقق من Context:
// في React DevTools → Components → AppProvider
```

### **النتيجة المتوقعة:**
```
✅ projectId: موجود
✅ publicAnonKey: موجود
✅ agreementAccepted: 'true' أو null
✅ userInfo: {...} أو null
✅ language: 'ar' أو 'en'
✅ theme: 'light' أو 'dark'
```

---

## 🎯 **نصائح الوقاية**

```
✅ احفظ نسخة احتياطية من localStorage بانتظام
✅ استخدم try-catch في جميع async functions
✅ سجّل الأخطاء في Console
✅ اختبر على متصفحات مختلفة
✅ اختبر على أجهزة مختلفة
✅ حدّث المتصفح باستمرار
✅ نظّف Cache بانتظام
```

---

# 🏆 **ملاحظة نهائية:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
معظم المشاكل يمكن حلها بـ:

1. localStorage.clear() + Reload
2. تحديث المتصفح
3. تحقق من اتصال الإنترنت
4. فحص Console للأخطاء

إذا استمرت المشكلة، استخدم نموذج بلاغ الخطأ أعلاه.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

**© 2025 جامعة الملك خالد - دليل حل المشاكل**  
**النسخة:** 2.0.0  
**آخر تحديث:** 12 نوفمبر 2025
