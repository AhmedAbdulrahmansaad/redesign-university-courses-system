# 🔐 دليل حل مشاكل تسجيل الدخول | Login Troubleshooting Guide

## المشكلة | The Problem

```
❌ Login error: AuthApiError: Invalid login credentials
```

## الأسباب المحتملة | Possible Causes

### 1. ❌ لم تسجل حساباً من قبل | You Haven't Registered Yet

**هذا هو السبب الأكثر شيوعاً!** | **This is the most common cause!**

**الحل** | **Solution:**
- اذهب إلى صفحة التسجيل (Sign Up)
- قم بإنشاء حساب جديد بنفس البريد الإلكتروني
- بعد التسجيل، حاول تسجيل الدخول مرة أخرى

### 2. ❌ البريد الإلكتروني خاطئ | Wrong Email

**المشكلة:** | **Problem:**
- أدخلت بريد إلكتروني مختلف عن الذي سجلت به
- خطأ إملائي في البريد الإلكتروني

**الحل:** | **Solution:**
- تأكد من كتابة البريد الإلكتروني بشكل صحيح
- استخدم نفس البريد الذي سجلت به

### 3. ❌ كلمة المرور خاطئة | Wrong Password

**المشكلة:** | **Problem:**
- أدخلت كلمة مرور خاطئة
- نسيت كلمة المرور

**الحل:** | **Solution:**
- تأكد من كتابة كلمة المرور بشكل صحيح
- انتبه لحالة الأحرف (Caps Lock)
- إذا نسيت كلمة المرور، استخدم "نسيت كلمة المرور"

### 4. ⚠️ حساب يتيم | Orphaned Account

**المشكلة:** | **Problem:**
- الحساب موجود في نظام المصادقة لكن ليس في قاعدة البيانات
- حدث خطأ أثناء التسجيل السابق

**الحل:** | **Solution:**
- النظام سينظف الحساب تلقائياً
- أو اذهب إلى صفحة "أدوات التنظيف" (Cleanup)
- بعد التنظيف، قم بالتسجيل من جديد

---

## 📝 خطوات حل المشكلة | Troubleshooting Steps

### الخطوة 1: تحقق أنك سجلت حساباً | Step 1: Verify Registration

```
❓ هل سبق لك التسجيل في النظام؟
❓ Have you registered in the system before?
```

- ✅ **نعم** → انتقل للخطوة 2
- ❌ **لا** → اذهب إلى صفحة التسجيل وأنشئ حساباً جديداً

### الخطوة 2: تحقق من البريد الإلكتروني | Step 2: Verify Email

```
✓ تأكد من كتابة البريد الإلكتروني بشكل صحيح
✓ Make sure the email is typed correctly
```

- استخدم نفس البريد الذي سجلت به
- تحقق من عدم وجود مسافات إضافية
- تأكد من امتداد البريد (مثال: @kku.edu.sa)

### الخطوة 3: تحقق من كلمة المرور | Step 3: Verify Password

```
✓ تأكد من كتابة كلمة المرور بشكل صحيح
✓ Make sure the password is typed correctly
```

- انتبه لحالة الأحرف (Caps Lock)
- تأكد من عدم وجود مسافات إضافية
- استخدم أيقونة العين 👁️ لإظهار كلمة المرور

### الخطوة 4: جرب أداة التنظيف | Step 4: Try Cleanup Tool

إذا كنت متأكداً من البيانات لكن ما زال الخطأ موجوداً:

1. اذهب إلى صفحة "أدوات التنظيف" | Go to "Cleanup" page
2. أدخل بريدك الإلكتروني | Enter your email
3. اضغط "تنظيف الحساب" | Click "Cleanup Account"
4. بعد التنظيف، سجل من جديد | After cleanup, register again

---

## 🎯 الحل السريع | Quick Fix

**الطريقة الأسرع لحل المشكلة:**

```bash
1. اذهب إلى صفحة التسجيل (Sign Up)
2. أنشئ حساباً جديداً
3. سجل الدخول بالبيانات الجديدة
```

**إذا كان البريد مستخدم من قبل:**

```bash
1. اذهب إلى صفحة "أدوات التنظيف"
2. نظف الحساب القديم
3. سجل من جديد
```

---

## ⚙️ للمطورين | For Developers

### كيف يعمل نظام المصادقة | How Authentication Works

```typescript
// 1. التحقق من البريد والباسورد في Supabase Auth
const { data, error } = await supabase.auth.signInWithPassword({
  email,
  password,
});

// 2. إذا نجح، جلب بيانات المستخدم من DB
const { data: userData } = await supabase
  .from('users')
  .select('*')
  .eq('auth_id', data.user.id)
  .single();
```

### الأخطاء المحتملة | Possible Errors

1. **Invalid credentials** (400)
   - السبب: بريد أو باسورد خاطئ
   - الحل: تحقق من البيانات

2. **User not found** (404)
   - السبب: حساب يتيم (موجود في Auth لكن ليس في DB)
   - الحل: تنظيف الحساب وإعادة التسجيل

3. **Missing student data** (400)
   - السبب: بيانات الطالب غير مكتملة
   - الحل: إعادة التسجيل مع بيانات كاملة

---

## 📞 الدعم الفني | Technical Support

إذا لم تنجح الحلول السابقة:

1. اتصل بالدعم الفني | Contact Support
2. قدم معلومات الخطأ الكاملة | Provide complete error details
3. أذكر الخطوات التي قمت بها | Mention steps you've tried

---

## ✅ التحقق من نجاح الحل | Verify Solution

بعد تطبيق الحل، يجب أن:

- ✅ تتمكن من تسجيل الدخول بنجاح
- ✅ ترى لوحة التحكم الخاصة بك
- ✅ لا ترى أي رسائل خطأ

---

**آخر تحديث:** نوفمبر 2024  
**Last Updated:** November 2024

**الإصدار:** v1.0  
**Version:** v1.0
