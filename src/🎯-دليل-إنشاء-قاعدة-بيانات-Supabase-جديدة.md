# 🎯 دليل إنشاء قاعدة بيانات Supabase جديدة من الصفر

## 📚 المحتوى
1. [إنشاء مشروع Supabase](#1-إنشاء-مشروع-supabase)
2. [تشغيل SQL لإنشاء الجداول](#2-تشغيل-sql-لإنشاء-الجداول)
3. [إضافة المقررات الدراسية](#3-إضافة-المقررات-الدراسية)
4. [إنشاء حساب المدير](#4-إنشاء-حساب-المدير)
5. [ربط التطبيق بقاعدة البيانات](#5-ربط-التطبيق-بقاعدة-البيانات)
6. [اختبار النظام](#6-اختبار-النظام)

---

## 1. إنشاء مشروع Supabase

### الخطوات:

#### أ. التسجيل في Supabase
1. اذهب إلى: **https://supabase.com**
2. اضغط **"Start your project"** أو **"Sign In"**
3. سجل دخول باستخدام:
   - GitHub (موصى به)
   - أو Google
   - أو Email

#### ب. إنشاء مشروع جديد
1. بعد تسجيل الدخول، اضغط **"New Project"**
2. املأ المعلومات:
   ```
   Organization: اختر أو أنشئ واحدة
   Project Name: kku-course-registration
   Database Password: [كلمة مرور قوية - احفظها!]
   Region: اختر الأقرب لك (مثل: Middle East - Bahrain)
   Pricing Plan: Free (مجاني)
   ```
3. اضغط **"Create new project"**
4. انتظر 2-3 دقائق حتى يتم إنشاء المشروع

#### ج. الحصول على بيانات الاتصال
بعد إنشاء المشروع، اذهب إلى:
- **Settings** (الإعدادات) → **API**
- احفظ المعلومات التالية:
  ```
  Project URL: https://xxxxxxxxxxxxx.supabase.co
  anon public key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  service_role key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  ```

⚠️ **مهم جداً:** احفظ هذه المعلومات في مكان آمن!

---

## 2. تشغيل SQL لإنشاء الجداول

### الخطوات:

#### أ. فتح SQL Editor
1. من لوحة تحكم Supabase
2. اذهب إلى **SQL Editor** (محرر SQL) من القائمة الجانبية
3. اضغط **"New query"** لإنشاء استعلام جديد

#### ب. نسخ ولصق SQL
1. افتح الملف: `/🔥-EXECUTE-THIS-SQL.sql` من مشروعك
2. **انسخ كل المحتوى** (Ctrl+A ثم Ctrl+C)
3. الصقه في SQL Editor في Supabase
4. اضغط **"Run"** أو اضغط **Ctrl+Enter**

#### ج. التحقق من النجاح
انتظر حتى يظهر:
```
Success. No rows returned
```

ثم تحقق من الجداول:
1. اذهب إلى **Table Editor** (محرر الجداول)
2. يجب أن تشاهد الجداول التالية:
   ```
   ✅ departments (الأقسام)
   ✅ users (المستخدمون)
   ✅ students (الطلاب)
   ✅ supervisors (المشرفون)
   ✅ admins (المدراء)
   ✅ courses (المقررات)
   ✅ course_plan (الخطة الدراسية)
   ✅ course_offers (عروض المقررات)
   ✅ registrations (التسجيلات)
   ✅ notifications (الإشعارات)
   ✅ reports (التقارير)
   ✅ agreements (التعهدات)
   ✅ ai_logs (سجل المساعد الذكي)
   ```

---

## 3. إضافة المقررات الدراسية

### خيار 1: عبر Backend API (موصى به) ✅

بعد ربط التطبيق بقاعدة البيانات الجديدة:
1. سجل دخول كمدير
2. اذهب إلى صفحة **"إدارة المقررات"**
3. استخدم زر **"استيراد المقررات من الخطة"**
4. سيتم إضافة جميع الـ 49 مقرراً تلقائياً

### خيار 2: عبر SQL مباشرة

إذا أردت إضافة المقررات يدوياً:

```sql
-- المستوى الأول - الفصل الأول
INSERT INTO courses (course_id, code, name_ar, name_en, credits, level, category, department_id) VALUES
('MIS-101', '101', 'مقدمة في نظم المعلومات', 'Introduction to Information Systems', 3, 1, 'متطلب قسم', 
 (SELECT id FROM departments WHERE code = 'MIS')),
 
('MATH-101', '101', 'الرياضيات للإدارة', 'Mathematics for Management', 3, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS')),
 
('ENG-101', '101', 'اللغة الإنجليزية (1)', 'English Language I', 3, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS')),
 
('ARAB-101', '101', 'اللغة العربية', 'Arabic Language', 2, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS')),
 
('ISLAM-101', '101', 'الثقافة الإسلامية (1)', 'Islamic Culture I', 2, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS')),
 
('PE-101', '101', 'التربية البدنية', 'Physical Education', 1, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS'));

-- المستوى الأول - الفصل الثاني
INSERT INTO courses (course_id, code, name_ar, name_en, credits, level, category, department_id, prerequisites) VALUES
('MIS-102', '102', 'برمجة الحاسب (1)', 'Computer Programming I', 3, 1, 'متطلب قسم',
 (SELECT id FROM departments WHERE code = 'MIS'), ARRAY['MIS-101']),
 
('STAT-102', '102', 'الإحصاء للإدارة', 'Statistics for Management', 3, 1, 'متطلب كلية',
 (SELECT id FROM departments WHERE code = 'MIS'), ARRAY['MATH-101']),
 
('MGMT-102', '102', 'مبادئ الإدارة', 'Principles of Management', 3, 1, 'متطلب كلية',
 (SELECT id FROM departments WHERE code = 'MIS'), NULL),
 
('ENG-102', '102', 'اللغة الإنجليزية (2)', 'English Language II', 3, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS'), ARRAY['ENG-101']),
 
('ISLAM-102', '102', 'الثقافة الإسلامية (2)', 'Islamic Culture II', 2, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS'), ARRAY['ISLAM-101']),
 
('COMM-102', '102', 'مهارات الاتصال', 'Communication Skills', 2, 1, 'متطلب جامعة',
 (SELECT id FROM departments WHERE code = 'MIS'), NULL);

-- ... (باقي المقررات - يمكنك إضافتها من predefinedCourses.ts)
```

💡 **نصيحة:** استخدم الخيار 1 (عبر واجهة التطبيق) لأنه أسرع وأسهل!

---

## 4. إنشاء حساب المدير

### طريقة 1: عبر التطبيق (موصى به) ✅

1. افتح صفحة **"إنشاء حساب"** في التطبيق
2. املأ البيانات:
   ```
   الاسم الكامل: د. محمد رشيد
   الرقم الوظيفي: 999000001
   البريد الإلكتروني: admin@kku.edu.sa
   كلمة المرور: Admin@123456
   الدور: مدير (Admin)
   ```
3. اضغط **"إنشاء الحساب"**
4. سجل دخول بنفس البيانات

### طريقة 2: عبر SQL مباشرة

```sql
-- 1. إنشاء المستخدم في Auth (استخدم Supabase Dashboard)
-- اذهب إلى Authentication → Users → Add User
-- Email: admin@kku.edu.sa
-- Password: Admin@123456
-- Auto Confirm: Yes

-- 2. بعد الحصول على auth_id، أضف السجل في الجداول:
-- احفظ الـ auth_id من Supabase Auth
-- ثم شغل هذا SQL (استبدل YOUR_AUTH_ID):

INSERT INTO users (auth_id, student_id, email, name, role, active)
VALUES (
  'YOUR_AUTH_ID_HERE',
  '999000001',
  'admin@kku.edu.sa',
  'د. محمد رشيد',
  'admin',
  true
);

-- 3. إضافة سجل في جدول admins
INSERT INTO admins (user_id, permissions)
VALUES (
  (SELECT id FROM users WHERE email = 'admin@kku.edu.sa'),
  '{"all": true, "manage_users": true, "manage_courses": true, "manage_registrations": true}'
);
```

---

## 5. ربط التطبيق بقاعدة البيانات

### الخطوات:

#### أ. تحديث ملف info.tsx
1. افتح الملف: `/utils/supabase/info.tsx`
2. استبدل القيم القديمة بالجديدة من خطوة 1:

```typescript
// القيم القديمة - استبدلها
export const projectId = 'xxxxxxxxxxxxx'; // ⬅️ من Project URL
export const publicAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'; // ⬅️ anon public key
```

#### ب. إضافة Environment Variables في Supabase
1. اذهب إلى مشروعك في Supabase
2. اذهب إلى **Settings** → **Edge Functions** → **Secrets**
3. أضف المتغيرات التالية:

```bash
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
OPENAI_API_KEY=sk-... (إذا كان لديك)
```

---

## 6. اختبار النظام

### الاختبارات الأساسية:

#### ✅ اختبار 1: إنشاء حساب طالب
```
الاسم: أحمد محمد علي
الرقم الجامعي: 442012345
البريد: ahmed442012345@kku.edu.sa
كلمة المرور: Test@123
التخصص: MIS
المستوى: 1
```

**النتيجة المتوقعة:**
- ✅ رسالة نجاح
- ✅ ظهور المستخدم في Auth
- ✅ ظهور السجل في جدول users
- ✅ ظهور السجل في جدول students

#### ✅ اختبار 2: تسجيل الدخول
```
البريد: ahmed442012345@kku.edu.sa
كلمة المرور: Test@123
```

**النتيجة المتوقعة:**
- ✅ تسجيل دخول ناجح
- ✅ توجيه إلى لوحة تحكم الطالب
- ✅ عرض البيانات الشخصية
- ✅ عدم ظهور "حسابك غير مكتمل" ✨

#### ✅ اختبار 3: إضافة مقرر
1. سجل دخول كطالب
2. اذهب إلى صفحة **"المقررات المتاحة"**
3. اضغط **"تسجيل"** على أي مقرر

**النتيجة المتوقعة:**
- ✅ رسالة نجاح
- ✅ ظهور المقرر في "مقرراتي"
- ✅ ظهور السجل في جدول registrations

#### ✅ اختبار 4: موافقة المشرف
1. أنشئ حساب مشرف
2. سجل دخول كمشرف
3. اذهب إلى **"طلبات التسجيل"**
4. وافق على طلب الطالب

**النتيجة المتوقعة:**
- ✅ تحديث حالة الطلب إلى "معتمد"
- ✅ إشعار للطالب

---

## 🎯 الخلاصة

### ما قمنا بفعله:
1. ✅ إنشاء مشروع Supabase جديد
2. ✅ إنشاء جميع الجداول (13 جدول)
3. ✅ إضافة المقررات الدراسية (49 مقرر)
4. ✅ إنشاء حساب المدير
5. ✅ ربط التطبيق بقاعدة البيانات الجديدة

### النتيجة:
🎉 **نظام نظيف 100% بدون أي مشاكل!**

- ❌ لا مستخدمين يتامى
- ❌ لا حسابات غير مكتملة
- ❌ لا أخطاء في تسجيل الدخول
- ✅ قاعدة بيانات حقيقية
- ✅ نظام احترافي جاهز

---

## 🆘 حل المشاكل الشائعة

### مشكلة 1: "حسابك غير مكتمل" لا تزال تظهر
**السبب:** لم يتم ربط التطبيق بقاعدة البيانات الجديدة
**الحل:**
1. تأكد من تحديث `/utils/supabase/info.tsx`
2. تأكد من إضافة Environment Variables
3. أعد تحميل الصفحة (Ctrl+F5)

### مشكلة 2: "Invalid JWT"
**السبب:** استخدام keys قديمة
**الحل:**
1. احصل على keys جديدة من Supabase Dashboard
2. حدّث `info.tsx` و Environment Variables
3. سجل خروج وسجل دخول من جديد

### مشكلة 3: "Table does not exist"
**السبب:** لم يتم تشغيل SQL بشكل صحيح
**الحل:**
1. افتح SQL Editor في Supabase
2. شغّل `/🔥-EXECUTE-THIS-SQL.sql` مرة أخرى
3. تحقق من Table Editor

### مشكلة 4: المقررات لا تظهر
**السبب:** لم يتم إضافة المقررات
**الحل:**
1. سجل دخول كمدير
2. اذهب إلى **"إدارة المقررات"**
3. اضغط **"استيراد المقررات من الخطة"**

---

## 📞 معلومات إضافية

### الملفات المرجعية:
- **Schema الكامل:** `/🔥-EXECUTE-THIS-SQL.sql`
- **المقررات:** `/components/pages/predefinedCourses.ts`
- **دليل الإصلاح السابق:** `/✅-حل-مشكلة-إنشاء-الحساب.md`

### روابط مفيدة:
- **Supabase Dashboard:** https://app.supabase.com
- **Supabase Docs:** https://supabase.com/docs
- **SQL Tutorial:** https://www.postgresql.org/docs/

---

## ✅ قائمة التحقق النهائية

قبل البدء في الاستخدام، تأكد من:

- [ ] إنشاء مشروع Supabase جديد
- [ ] حفظ Project URL و API Keys
- [ ] تشغيل SQL لإنشاء الجداول
- [ ] التحقق من ظهور جميع الجداول في Table Editor
- [ ] إضافة المقررات الدراسية
- [ ] إنشاء حساب المدير
- [ ] تحديث `/utils/supabase/info.tsx`
- [ ] إضافة Environment Variables
- [ ] اختبار إنشاء حساب طالب
- [ ] اختبار تسجيل الدخول
- [ ] اختبار إضافة مقرر

---

**تاريخ الإنشاء:** 28 نوفمبر 2025  
**الحالة:** ✅ جاهز للتنفيذ  
**المدة المتوقعة:** 15-20 دقيقة

🎓 **نظام تسجيل المقررات - جامعة الملك خالد**  
✨ **نظيف • احترافي • جاهز للإنتاج**
