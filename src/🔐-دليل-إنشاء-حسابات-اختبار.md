# 🔐 دليل إنشاء حسابات اختبار

## 📋 نظرة عامة

هذا الدليل يوضح كيفية إنشاء حسابات اختبار في النظام للتأكد من عمل تسجيل الدخول بشكل صحيح.

---

## ⚠️ سبب المشكلة الحالية

إذا ظهرت لك رسالة خطأ عند تسجيل الدخول:

```
❌ Login error: Invalid login credentials
```

**السبب المحتمل:**
1. ✅ لا يوجد حساب مسجل بهذه البيانات في Supabase Auth
2. ✅ كلمة المرور غير صحيحة
3. ✅ الحساب تم حذفه من Auth لكن موجود في قاعدة البيانات

---

## 🎯 الحلول

### **الحل 1: إنشاء حساب جديد عبر صفحة التسجيل**

#### **الخطوات:**

1️⃣ **اذهب إلى صفحة التسجيل**
```
- افتح الموقع
- اضغط "تسجيل جديد" أو "Sign Up"
```

2️⃣ **أدخل البيانات**

**لطالب:**
```
الاسم الكامل: أحمد محمد علي
الرقم الجامعي: 420123456
البريد الإلكتروني: ahmad.mohammed@kku.edu.sa
رقم الجوال: 0501234567
كلمة المرور: Student@123
تأكيد كلمة المرور: Student@123

التخصص: نظم المعلومات الإدارية
المستوى الدراسي: 1
المعدل التراكمي (اختياري): 4.50
```

**لمشرف:**
```
الاسم الكامل: د. محمد أحمد
الرقم الوظيفي: (اتركه فارغ - سيتم توليده تلقائياً)
البريد الإلكتروني: dr.mohammed@kku.edu.sa
رقم الجوال: 0507654321
كلمة المرور: Supervisor@123
تأكيد كلمة المرور: Supervisor@123

الدور: مشرف أكاديمي
```

**لمدير:**
```
الاسم الكامل: إدارة النظام
البريد الإلكتروني: admin@kku.edu.sa
كلمة المرور: Admin@123
الدور: مدير نظام
```

3️⃣ **اضغط "تسجيل"**
```
✅ يجب أن تظهر رسالة نجاح
✅ سيتم تحويلك لصفحة تسجيل الدخول
```

4️⃣ **سجل دخول**
```
الرقم الجامعي/الوظيفي: (استخدم الرقم الذي أدخلته)
أو البريد الإلكتروني: (استخدم الإيميل الذي أدخلته)
كلمة المرور: (استخدم كلمة المرور الذي أدخلتها)
```

---

### **الحل 2: استخدام حساب المدير الافتراضي (إذا كان موجود)**

إذا كنت قد أنشأت حساب مدير من قبل:

```
البريد الإلكتروني: admin@kku.edu.sa
كلمة المرور: Admin@123
```

أو:

```
الرقم الوظيفي: ADM001
كلمة المرور: Admin@123
```

---

### **الحل 3: إنشاء حسابات عبر SQL مباشرة (للمطورين فقط)**

إذا كنت تملك صلاحيات Supabase:

#### **1. إنشاء حساب مدير:**

```sql
-- أولاً: إنشاء حساب في Supabase Auth عبر Dashboard
-- Authentication > Users > Add User
-- Email: admin@kku.edu.sa
-- Password: Admin@123
-- Auto Confirm Email: Yes

-- ثانياً: احصل على auth_id من Auth Dashboard
-- ثم نفذ هذا الـ SQL:

-- إنشاء قسم (إذا لم يكن موجود)
INSERT INTO departments (code, name_ar, name_en, description_ar, description_en, active)
VALUES ('MIS', 'نظم المعلومات الإدارية', 'Management Information Systems', 'قسم نظم المعلومات الإدارية', 'MIS Department', true)
ON CONFLICT (code) DO NOTHING;

-- إنشاء مستخدم
INSERT INTO users (auth_id, student_id, email, name, phone, role, department_id, active)
VALUES (
  'AUTH_ID_FROM_DASHBOARD', -- استبدل بالـ auth_id من Dashboard
  'ADM001',
  'admin@kku.edu.sa',
  'إدارة النظام',
  '0501234567',
  'admin',
  (SELECT id FROM departments WHERE code = 'MIS'),
  true
);

-- إنشاء سجل في جدول admins
INSERT INTO admins (user_id, permissions, can_manage_users, can_manage_courses, can_manage_registrations)
VALUES (
  (SELECT id FROM users WHERE email = 'admin@kku.edu.sa'),
  jsonb_build_array('all'),
  true,
  true,
  true
);
```

#### **2. إنشاء حساب طالب:**

```sql
-- أولاً: إنشاء في Auth Dashboard
-- Email: student@kku.edu.sa
-- Password: Student@123

-- ثانياً: نفذ SQL:
INSERT INTO users (auth_id, student_id, email, name, phone, role, department_id, active)
VALUES (
  'AUTH_ID_FROM_DASHBOARD',
  '420123456',
  'student@kku.edu.sa',
  'أحمد محمد علي',
  '0509876543',
  'student',
  (SELECT id FROM departments WHERE code = 'MIS'),
  true
);

INSERT INTO students (user_id, level, gpa, total_credits, completed_credits, major, status, enrollment_year, expected_graduation_year)
VALUES (
  (SELECT id FROM users WHERE email = 'student@kku.edu.sa'),
  1,
  0.0,
  0,
  0,
  'Management Information Systems',
  'active',
  2024,
  2028
);
```

---

## 🧪 اختبار تسجيل الدخول

بعد إنشاء الحساب:

### **1. باستخدام الرقم الجامعي:**
```
الرقم الجامعي: 420123456
كلمة المرور: Student@123
```

### **2. باستخدام البريد الإلكتروني:**
```
البريد الإلكتروني: student@kku.edu.sa
كلمة المرور: Student@123
```

### **3. التحقق من النجاح:**
```
✅ يجب أن تظهر رسالة: "مرحباً [الاسم]!"
✅ يتم تحويلك للوحة التحكم المناسبة
✅ لا توجد أخطاء في Console
```

---

## 🔍 استكشاف الأخطاء الشائعة

### **الخطأ 1: "الرقم الجامعي/الوظيفي غير موجود في النظام"**

**السبب:**
- الحساب غير موجود في جدول `users`

**الحل:**
- أنشئ حساب جديد عبر صفحة التسجيل

---

### **الخطأ 2: "كلمة المرور غير صحيحة"**

**السبب:**
- كلمة المرور المدخلة لا تطابق كلمة المرور في Supabase Auth

**الحل:**
- تأكد من كلمة المرور
- أو أعد تعيين كلمة المرور من Supabase Dashboard

---

### **الخطأ 3: "حسابك معطّل"**

**السبب:**
- الحقل `active` في جدول `users` = `false`

**الحل:**
```sql
UPDATE users 
SET active = true 
WHERE student_id = 'YOUR_STUDENT_ID';
```

---

### **الخطأ 4: "حسابك موجود في Auth لكن غير موجود في قاعدة البيانات"**

**السبب:**
- الحساب موجود في Supabase Auth لكن غير موجود في جدول `users`

**الحل:**
- احذف الحساب من Auth وأعد التسجيل
- أو أضف السجل في جدول `users` يدوياً

---

## 📊 التحقق من الحسابات الموجودة

### **عبر SQL:**

```sql
-- عرض جميع المستخدمين
SELECT 
  u.student_id,
  u.email,
  u.name,
  u.role,
  u.active,
  s.level,
  s.major,
  s.gpa
FROM users u
LEFT JOIN students s ON s.user_id = u.id
WHERE u.active = true
ORDER BY u.created_at DESC;

-- عرض المشرفين والمدراء
SELECT 
  student_id,
  email,
  name,
  role,
  active
FROM users
WHERE role IN ('supervisor', 'admin')
  AND active = true;
```

---

## 🎯 حسابات اختبار مقترحة

### **1. حساب مدير:**
```
Email: admin@kku.edu.sa
Password: Admin@123
Student ID: ADM001
Role: admin
```

### **2. حساب مشرف:**
```
Email: supervisor@kku.edu.sa
Password: Supervisor@123
Student ID: SUP001
Role: supervisor
```

### **3. حساب طالب - المستوى الأول:**
```
Email: student1@kku.edu.sa
Password: Student@123
Student ID: 420123456
Role: student
Level: 1
Major: MIS
```

### **4. حساب طالب - المستوى الثالث:**
```
Email: student3@kku.edu.sa
Password: Student@123
Student ID: 420123457
Role: student
Level: 3
Major: MIS
GPA: 3.75
```

---

## ✅ قائمة التحقق

قبل تسجيل الدخول، تأكد من:

- [ ] الحساب موجود في Supabase Auth
- [ ] الحساب موجود في جدول `users`
- [ ] الحقل `active` = `true`
- [ ] الحقل `auth_id` في جدول `users` يطابق auth_id في Auth
- [ ] كلمة المرور صحيحة
- [ ] البريد الإلكتروني مؤكد (email_confirm: true)

---

## 📞 الدعم

إذا استمرت المشكلة:

1. **افتح Console (F12)** واطلع على الأخطاء
2. **تحقق من Backend Logs** في Supabase Dashboard
3. **راجع جدول `users`** وتأكد من البيانات
4. **تحقق من Auth Users** في Supabase Dashboard

---

**تم التحديث:** نوفمبر 2024  
**الإصدار:** 2.0
