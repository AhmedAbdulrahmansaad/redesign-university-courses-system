# 🧪 خطوات اختبار شاملة للنظام

## 🎯 الهدف
التأكد من أن النظام مرتبط بالكامل مع Supabase وجميع العمليات تعمل بشكل حقيقي.

---

## 📋 قائمة الاختبارات

### ✅ الاختبار 1: المقررات المتاحة للطلاب
### ✅ الاختبار 2: التسجيل في مقرر
### ✅ الاختبار 3: موافقة المشرف
### ✅ الاختبار 4: Dashboard الطالب
### ✅ الاختبار 5: إضافة مقرر (المدير)
### ✅ الاختبار 6: إضافة مشرف
### ✅ الاختبار 7: Dashboard المدير

---

## 🧪 الاختبار 1: المقررات المتاحة للطلاب

### الهدف:
التأكد من أن الطلاب يرون المقررات المتاحة من قاعدة البيانات.

### الخطوات:

```bash
1. افتح المتصفح في وضع Incognito/Private
2. اذهب إلى الموقع
3. اضغط "تسجيل دخول"
4. أدخل بيانات طالب:
   - الرقم الجامعي: 442100001
   - كلمة المرور: password
5. اضغط "تسجيل الدخول"
```

### ✅ النتائج المتوقعة:

```
✅ يتم توجيهك إلى Student Dashboard
✅ تظهر بيانات الطالب الحقيقية (الاسم، المستوى، GPA)
```

### الآن اختبر المقررات:

```bash
6. اضغط "المقررات المتاحة" من القائمة
7. انتظر التحميل
```

### ✅ النتائج المتوقعة:

```
✅ تظهر قائمة المقررات (49 مقرر)
✅ كل مقرر يحتوي على:
   - رمز المقرر (مثل: MIS100)
   - الاسم العربي والإنجليزي
   - عدد الساعات
   - المستوى
   - زر "تسجيل"
✅ يمكنك البحث عن مقرر
✅ يمكنك الفلترة حسب المستوى
```

### 🔍 للتأكد:

افتح Console (F12) وابحث عن:
```
✅ "📚 Fetching courses for user:"
✅ "📚 Response status: 200"
✅ "✅ Loaded XX courses"
```

---

## 🧪 الاختبار 2: التسجيل في مقرر

### الهدف:
التأكد من أن عملية التسجيل تُحفظ في قاعدة البيانات.

### الخطوات:

```bash
1. (تكملة من الاختبار 1 - لا تزال في صفحة المقررات)
2. اختر أي مقرر من المستوى 1
3. اضغط زر "تسجيل" / "Register"
4. انتظر
```

### ✅ النتائج المتوقعة:

```
✅ تظهر رسالة نجاح:
   "✅ تم إرسال طلب تسجيل [اسم المقرر] للمشرف الأكاديمي"
✅ الزر يتغير إلى "جاري التسجيل..." أثناء العملية
✅ بعد الانتهاء، الزر يعود لحالته الطبيعية
```

### 🔍 للتأكد:

افتح Console (F12) وابحث عن:
```
✅ "📝 Registering for course: course-XXX"
✅ "📝 Registration response: {success: true}"
```

### الآن اختبر الحفظ:

```bash
5. اذهب إلى Dashboard (اضغط "العودة للوحة التحكم")
6. تحقق من بطاقة "المقررات المعلقة"
```

### ✅ النتائج المتوقعة:

```
✅ العدد زاد بمقدار 1
✅ المقرر الذي سجلته يظهر في القائمة بحالة "قيد الموافقة"
```

---

## 🧪 الاختبار 3: موافقة المشرف

### الهدف:
التأكد من أن المشرف يرى الطلبات ويمكنه الموافقة عليها.

### الخطوات:

```bash
1. سجّل خروج من حساب الطالب
2. سجّل دخول كمشرف:
   - الرقم الجامعي: supervisor1
   - كلمة المرور: password
3. اذهب إلى "طلبات التسجيل"
```

### ✅ النتائج المتوقعة:

```
✅ تظهر قائمة الطلبات المعلقة
✅ الطلب الذي قدمته كطالب يظهر في القائمة
✅ تظهر تفاصيل الطلب:
   - اسم الطالب
   - رقمه الجامعي
   - المقرر
   - الساعات
   - تاريخ الطلب
✅ يوجد زرّا "موافقة" و "رفض"
```

### الآن اختبر الموافقة:

```bash
4. اضغط زر "موافقة" ✅ على الطلب
5. انتظر
```

### ✅ النتائج المتوقعة:

```
✅ تظهر رسالة نجاح: "تمت الموافقة على الطلب"
✅ الطلب يختفي من القائمة
✅ عداد "الطلبات المعلقة" ينقص بمقدار 1
```

---

## 🧪 الاختبار 4: Dashboard الطالب

### الهدف:
التأكد من أن الإحصائيات في Dashboard محسوبة من قاعدة البيانات.

### الخطوات:

```bash
1. سجّل خروج من حساب المشرف
2. سجّل دخول كطالب مرة أخرى
   - الرقم الجامعي: 442100001
   - كلمة المرور: password
3. اذهب إلى Dashboard
```

### ✅ النتائج المتوقعة:

```
✅ بطاقة "الساعات المسجلة":
   - العدد صحيح (3 ساعات مثلاً، بناءً على المقرر المعتمد)
   
✅ بطاقة "المقررات المسجلة":
   - العدد صحيح (1 مقرر)
   
✅ بطاقة "المعدل التراكمي":
   - GPA يظهر (حتى لو 0.00 في البداية)
   
✅ بطاقة "المقررات المعلقة":
   - العدد = 0 (لأن المشرف وافق)
   
✅ قائمة "المقررات المسجلة":
   - المقرر الذي سجلته يظهر
   - الحالة: "معتمد" / "Approved"
   - الساعات تظهر بشكل صحيح
```

### 🔍 للتأكد:

افتح Console (F12) وابحث عن:
```
✅ "📊 [Dashboard] Fetching statistics from server..."
✅ "✅ [Dashboard] Server statistics: {...}"
✅ totalApprovedCourses > 0
✅ totalCreditHours > 0
```

---

## 🧪 الاختبار 5: إضافة مقرر (المدير)

### الهدف:
التأكد من أن المدير يمكنه إضافة مقرر جديد ويُحفظ في قاعدة البيانات.

### الخطوات:

```bash
1. سجّل خروج من حساب الطالب
2. سجّل دخول كمدير:
   - الرقم الجامعي: admin
   - كلمة المرور: admin123
3. اذهب إلى "إدارة المقررات"
4. اضغط "إضافة مقرر جديد" ➕
5. املأ البيانات:
   - رمز المقرر: TEST101
   - الاسم العربي: مقرر تجريبي
   - الاسم الإنجليزي: Test Course
   - الساعات المعتمدة: 3
   - المستوى: 1
   - القسم: MIS
   - الوصف (اختياري)
6. اضغط "إضافة" ✅
```

### ✅ النتائج المتوقعة:

```
✅ تظهر رسالة نجاح: "✅ تم إضافة المقرر بنجاح"
✅ المقرر يظهر فوراً في القائمة
✅ يمكنك رؤية المقرر الجديد في الجدول
```

### الآن تأكد من الحفظ:

```bash
7. حدّث الصفحة (F5)
8. تحقق من القائمة
```

### ✅ النتائج المتوقعة:

```
✅ المقرر لا يزال موجوداً (محفوظ في KV Store)
✅ جميع البيانات صحيحة
```

### اختبر الظهور للطلاب:

```bash
9. سجّل خروج
10. سجّل دخول كطالب
11. اذهب إلى "المقررات المتاحة"
12. ابحث عن TEST101
```

### ✅ النتائج المتوقعة:

```
✅ المقرر الجديد يظهر للطالب
✅ يمكن التسجيل فيه
```

---

## 🧪 الاختبار 6: إضافة مشرف

### الهدف:
التأكد من أن المدير يمكنه إضافة مشرف جديد ويُحفظ في قاعدة البيانات.

### الخطوات:

```bash
1. سجّل دخول كمدير
2. اذهب إلى "إدارة المشرفين"
3. اضغط "إضافة مشرف جديد" ➕
4. املأ البيانات:
   - الاسم الكامل: أ. أحمد محمد علي
   - الرقم الوظيفي: SUP001
   - البريد الإلكتروني: ahmed.ali@kku.edu.sa
   - رقم الجوال: 0501234567
   - القسم: نظم المعلومات الإدارية
   - كلمة المرور: test123
5. اضغط "إضافة" ✅
```

### ✅ النتائج المتوقعة:

```
✅ تظهر رسالة نجاح: "✅ تم إضافة المشرف بنجاح"
✅ المشرف يظهر فوراً في القائمة
✅ الحالة: "نشط" / "Active"
```

### الآن اختبر تسجيل الدخول:

```bash
6. سجّل خروج
7. سجّل دخول بحساب المشرف الجديد:
   - الإيميل: ahmed.ali@kku.edu.sa
   - كلمة المرور: test123
8. يجب أن تصل إلى Supervisor Dashboard
```

### ✅ النتائج المتوقعة:

```
✅ تسجيل الدخول ناجح
✅ يتم توجيهك إلى Supervisor Dashboard
✅ ترى قائمة "طلبات التسجيل"
✅ ترى فقط طلبات قسمك (MIS)
```

---

## 🧪 الاختبار 7: Dashboard المدير

### الهدف:
التأكد من أن الإحصائيات في Dashboard المدير محسوبة من قاعدة البيانات.

### الخطوات:

```bash
1. سجّل دخول كمدير
2. اذهب إلى Admin Dashboard
3. راقب الإحصائيات
```

### ✅ النتائج المتوقعة:

```
✅ بطاقة "إجمالي الطلاب":
   - عدد > 0 (حسب عدد الطلاب المسجلين)
   
✅ بطاقة "إجمالي المقررات":
   - عدد >= 49 (+ المقررات التي أضفتها)
   
✅ بطاقة "طلبات التسجيل":
   - عدد >= 0 (حسب الطلبات المعلقة)
   
✅ بطاقة "المشرفين":
   - عدد >= 1 (+ المشرف الذي أضفته)
```

### 🔍 للتأكد:

افتح Console (F12) وابحث عن:
```
✅ طلب إلى /admin/stats
✅ استجابة بـ status 200
✅ البيانات تحتوي على:
   - totalStudents
   - totalCourses
   - pendingRequests
   - totalSupervisors
```

---

## 🎯 الخلاصة

### إذا نجحت جميع الاختبارات:

```
✅ النظام مرتبط بالكامل مع Supabase
✅ لا توجد بيانات وهمية
✅ جميع العمليات تُحفظ في قاعدة البيانات
✅ CRUD كامل يعمل
✅ نظام الصلاحيات يعمل بشكل صحيح
✅ الإحصائيات محسوبة بشكل حقيقي
✅ النظام جاهز للاستخدام الفعلي
```

---

## 🐛 إذا فشل اختبار:

### افتح Console (F12) وابحث عن:

```
❌ رسائل خطأ حمراء
❌ "Failed to fetch"
❌ "Unauthorized"
❌ "Error: ..."
```

### ثم:

1. انسخ رسالة الخطأ الكاملة
2. انسخ URL الطلب
3. انسخ Response إن وُجدت
4. شاركها معي لإصلاحها

---

## 📊 نموذج التقرير

بعد إكمال الاختبارات، املأ هذا النموذج:

```
✅ الاختبار 1: المقررات المتاحة
   النتيجة: ☐ نجح  ☐ فشل
   ملاحظات: ________________

✅ الاختبار 2: التسجيل في مقرر
   النتيجة: ☐ نجح  ☐ فشل
   ملاحظات: ________________

✅ الاختبار 3: موافقة المشرف
   النتيجة: ☐ نجح  ☐ فشل
   ملاحظات: ________________

✅ الاختبار 4: Dashboard الطالب
   النتيجة: ☐ نجح  ☐ فشل
   ملاحظات: ________________

✅ الاختبار 5: إضافة مقرر
   النتيجة: ☐ نجح  ☐ فشل
   ملاحظات: ________________

✅ الاختبار 6: إضافة مشرف
   النتيجة: ☐ نجح  ☐ فشل
   ملاحظات: ________________

✅ الاختبار 7: Dashboard المدير
   النتيجة: ☐ نجح  ☐ فشل
   ملاحظات: ________________
```

---

**التاريخ:** 17 نوفمبر 2025  
**المدة المتوقعة:** 30-45 دقيقة  
**المستوى:** شامل
